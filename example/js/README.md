# Finch Connect SDK - JavaScript Example

Try out the Finch Connect SDK with an example JavaScript app.

## Run

This example is a standalone pnpm package. Run `corepack enable` once, then from this directory:

```bash
pnpm install
pnpm run start
```